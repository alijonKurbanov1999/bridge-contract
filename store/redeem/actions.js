export default {
  // confirmRedeem({ commit }, { sender, recipient, amount, nonce, chainFrom, symbol, v, r, s }) {
  //   console.log('success from store actions: ', nonce, amount, symbol)
  //   commit('modals/CONFIRMATION', {sender, recipient, amount, nonce, chainFrom, symbol, v, r, s}, {root: true})
  //   // dispatch('modals/redeem', {}, {root: true} )
  // }
}
